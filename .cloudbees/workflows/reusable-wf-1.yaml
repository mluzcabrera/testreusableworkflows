apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: reusable wf 1

on:
  workflow_call: {}

jobs:
  JobName1:
    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world"
