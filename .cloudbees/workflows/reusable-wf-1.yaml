apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: reusable wf 1

on:
  workflow_call: {}

jobs:
  image:
    steps:
      - uses: docker://alpine:3.20
        run: |
          echo "Reusable workflows"
