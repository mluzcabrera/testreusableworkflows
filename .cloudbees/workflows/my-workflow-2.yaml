apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  workflow_dispatch:
jobs:
  call_rw_2:
    uses: mluzcabrera/testreusableworkflows/.cloudbees/workflows/reusable-wf-2.yaml@v1