apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: reusable wf 2
on:
  workflow_call:
  workflow_dispatch:
jobs:
  call_rw:
    uses: mluzcabrera/testreusableworkflows/.cloudbees/workflows/reusable-wf-3.yaml
